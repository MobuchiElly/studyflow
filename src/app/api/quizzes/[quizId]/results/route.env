// import { NextResponse } from 'next/server';
// import { createClient } from '@/lib/supabase/server';
// import { QuizResultSchema } from '@/types/quiz';
// import { ZodError } from 'zod';

// export async function POST(
//   request: Request,
//   { params }: { params: { quizId: string } }
// ) {
//   const supabase = createClient();
//   const {
//     data: { user },
//   } = await supabase.auth.getUser();

//   if (!user) {
//     return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
//   }

//   const quizId = params.quizId;

//   try {
//     const body = await request.json();
//     const validatedData = QuizResultSchema.parse(body);

//     // Ensure the quiz_id in the body matches the route parameter
//     if (validatedData.quiz_id !== quizId) {
//       return NextResponse.json({ error: 'Quiz ID mismatch' }, { status: 400 });
//     }

//     const { data, error } = await supabase
//       .from('quiz_results')
//       .insert({
//         user_id: user.id,
//         quiz_id: validatedData.quiz_id,
//         score: validatedData.score,
//         total_questions: validatedData.total_questions,
//         answers: validatedData.answers,
//       })
//       .select()
//       .single();

//     if (error) {
//       console.error('Supabase insert error:', error);
//       return NextResponse.json({ error: error.message }, { status: 500 });
//     }

//     return NextResponse.json(data, { status: 201 });
//   } catch (error) {
//     if (error instanceof ZodError) {
//       return NextResponse.json({ error: error.errors }, { status: 400 });
//     }
//     console.error('Unexpected error:', error);
//     return NextResponse.json(
//       { error: 'Internal Server Error' },
//       { status: 500 }
//     );
//   }
// }

// export async function GET(
//   request: Request,
//   { params }: { params: { quizId: string } }
// ) {
//   const supabase = createClient();
//   const {
//     data: { user },
//   } = await supabase.auth.getUser();

//   if (!user) {
//     return NextResponse.json({ error: 'Unauthorized' }, { status: 401 });
//   }

//   const quizId = params.quizId;

//   try {
//     const { data, error } = await supabase
//       .from('quiz_results')
//       .select('*')
//       .eq('quiz_id', quizId)
//       .eq('user_id', user.id)
//       .order('created_at', { ascending: false });

//     if (error) {
//       console.error('Supabase fetch error:', error);
//       return NextResponse.json({ error: error.message }, { status: 500 });
//     }

//     return NextResponse.json(data, { status: 200 });
//   } catch (error) {
//     console.error('Unexpected error:', error);
//     return NextResponse.json(
//       { error: 'Internal Server Error' },
//       { status: 500 }
//     );
//   }
// }