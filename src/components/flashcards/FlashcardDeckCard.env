// import React from 'react';
// import { FlashcardDeck } from '@/types/flashcard';
// import { Card, CardContent, CardDescription, CardFooter, CardHeader, CardTitle } from '@/components/ui/card';
// import { Button } from '@/components/ui/button';
// import { Edit, Trash2, BookOpen } from 'lucide-react';
// import Link from 'next/link';

// interface FlashcardDeckCardProps {
//   deck: FlashcardDeck;
//   onEdit: (deck: FlashcardDeck) => void;
//   onDelete: (id: string) => void;
//   isDeleting: boolean;
// }

// const FlashcardDeckCard: React.FC<FlashcardDeckCardProps> = ({ deck, onEdit, onDelete, isDeleting }) => {
//   return (
//     <Card className="flex flex-col">
//       <CardHeader>
//         <CardTitle>{deck.title}</CardTitle>
//         <CardDescription>{deck.description}</CardDescription>
//       </CardHeader>
//       <CardContent className="flex-grow">
//         <p className="text-sm text-gray-500">Created: {new Date(deck.created_at).toLocaleDateString()}</p>
//         {deck.updated_at && (
//           <p className="text-sm text-gray-500">Last Updated: {new Date(deck.updated_at).toLocaleDateString()}</p>
//         )}
//       </CardContent>
//       <CardFooter className="flex justify-end space-x-2">
//         <Button variant="outline" size="sm" onClick={() => onEdit(deck)}>
//           <Edit className="h-4 w-4 mr-2" />
//           Edit
//         </Button>
//         <Button variant="destructive" size="sm" onClick={() => onDelete(deck.id)} disabled={isDeleting}>
//           {isDeleting ? 'Deleting...' : <Trash2 className="h-4 w-4 mr-2" />}
//           {isDeleting ? '' : 'Delete'}
//         </Button>
//         <Link href={`/dashboard/flashcards/${deck.id}`}>
//           <Button size="sm">
//             <BookOpen className="h-4 w-4 mr-2" />
//             View Cards
//           </Button>
//         </Link>
//       </CardFooter>
//     </Card>
//   );
// };

// export default FlashcardDeckCard;